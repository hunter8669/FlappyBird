# 云部署核心依赖 - 简化版
# 只包含运行HTTP服务器必需的库

# HTTP请求处理 - 标准库已足够
# 无需额外依赖，使用Python内置的http.server

# 如果需要更强大的功能，可以解除以下注释：
# fastapi>=0.100.0
# uvicorn[standard]>=0.20.0

# 文件处理和JSON支持 - 标准库已包含
# 日期时间处理 - 标准库已包含 